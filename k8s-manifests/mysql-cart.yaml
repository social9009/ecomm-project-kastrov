apiVersion: v1
kind: Service
metadata:
  name: mysql-cart
spec:
  clusterIP: None
  selector:
    app: mysql-cart
  ports:
  - port: 3306
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-cart
spec:
  serviceName: mysql-cart
  replicas: 1
  selector:
    matchLabels:
      app: mysql-cart
  template:
    metadata:
      labels:
        app: mysql-cart
    spec:
      containers:
      - name: mysql
        image: mysql:8
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: password
        - name: MYSQL_DATABASE
          value: cartdb
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
  volumeClaimTemplates:
  - metadata:
      name: mysql-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      storageClassName: gp2
      resources:
        requests:
          storage: 5Gi
